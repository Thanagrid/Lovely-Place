<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lovely Place | ADMIN - Post Management</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <link href="https://fonts.googleapis.com/css2?family=Kanit&display=swap" rel="stylesheet">
    <style>
        *{
            font-family: 'Kanit', sans-serif;
        }

    </style>
        
</head>
<body>
    
    <%- include ('navbar.ejs') %>

    <div class="container mt-5">
        <h2 class="mb-2">จัดการโพสต์</h2>
        <p>เรียกดูรายละเอียด และจัดการโพสต์ทั้งหมด!!</p>
        <div class="table-responsive">
            <table class="table table-hover table-bordered table-light">
                <thead>
                    <tr class="table-dark">
                        <th class="text-center">ID</th>
                        <th>ชื่อโพสต์</th>
                        <th>คำอธิบายย่อ</th>
                        <th class="text-center">ผู้โพสต์</th>
                        <th class="text-center">ดู</th>
                        <th class="text-center">ลบ</th>
                    </tr>
                </thead>
                <tbody>
                    <% Object.values(postData).forEach((item)=>{ %>
                    <tr>
                        <td class="text-center"><%= item.post_id %> </td>
                        <td><%= item.post_title %> </td>
                        <td><%= item.post_preDes %></td>
                        <td class="text-center"><%= item.user_id %> </td>
                        <!-- ดูโพสต์ -->
                        <td>
                            <a href="/post/<%= item.post_id %>" class="btn btn-success col-12">ดู</a>
                        </td>
                        <!-- ลบโพสต์ -->
                        <td>
                            <form method="post" action="/admin_management/posts/delete">
                                <input type="hidden" value="<%= item.post_id %>" name="post_id">
                                <button type="sumbit" class="btn btn-danger col-12" onclick="return confirm('คุณต้องการลบโพสต์หรือไม่')">ลบ</button>
                            </form>
                        </td>
                    </tr>
                    <% }); %>
                </tbody>
            </table>
        </div>
           
    </div>

</body>
</html>
